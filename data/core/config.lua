return {

    fps = 60,
    project_scan_rate = 5,

    max_log_items = 80,
    file_size_limit = 10,
    message_timeout = 3,
    mouse_wheel_scroll = 50 * SCALE,

    ignore_files = "^%.",
    symbol_pattern = "[%a_][%w_]*",
    non_word_chars = " \t\n/\\()\"':,.;<>~!@#$%^&*|+=[]{}`?-",

    undo_merge_timeout = 0.3,
    max_undos = 10000,

    line_height = 1.2,

    indent_size = 2,
    tab_type = "soft",

    line_limit = 80,
    max_symbols = 1024,

    highlight_current_line = true,
}
